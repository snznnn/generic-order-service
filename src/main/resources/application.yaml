spring:
  application:
    name: generic-order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: mypass
    username: postgres
    hikari:
      schema: order
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081